name: FertiLitef
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.1

environment:
  sdk: '>=3.9.0 <4.0.0'
  
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0



flutter:
  uses-material-design: true
  assets:
    - assets/synthetic_soil_and_plant_data_s2_pattern.csv
    - assets/
  # The 'generate: true' line is for older versions of the
  # flutter_gen or flutter_localizations package and can often be omitted.
  # If it causes no issue, leave it. If it fails, remove it too.
  generate: true
  # REMOVED: tree-shake-icons: false
